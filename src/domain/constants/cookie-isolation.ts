/**
 * Cookie isolation constants.
 *
 * @module domain/constants/cookie-isolation
 */

/** Time-to-live for isolated cookies in milliseconds (30 minutes). */
export const COOKIE_TTL_MS: number = 30 * 60 * 1000;
