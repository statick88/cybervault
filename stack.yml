# =============================================================================
# CyberVault Stack (Docker Swarm)
# =============================================================================
# Deploy: docker stack deploy -c stack.yml cybervault
# Remove: docker stack rm cybervault
# =============================================================================

version: "3.8"

services:
  # -----------------------------------------------------------------------------
  # API Server
  # -----------------------------------------------------------------------------
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    image: cybervault-api:latest
    container_name: cybervault-api
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=cybervault
      - DB_USER=cybervault
      - DB_PASSWORD=${POSTGRES_PASSWORD:-cybervault_secret}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-cybervault_redis_secret}
      - JWT_SECRET=${JWT_SECRET:-SuperSecretJWTKey12345678901234567890}
      - VAULT_MASTER_KEY=${VAULT_MASTER_KEY:-a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2}
      - VAULT_ENCRYPTION_SALT=${VAULT_ENCRYPTION_SALT:-abcdef1234567890abcdef1234567890}
      - IPFS_ENABLED=false
      - USE_POSTGRES=true
      - DATABASE_URL=postgresql://cybervault:${POSTGRES_PASSWORD:-cybervault_secret}@postgres:5432/cybervault
    depends_on:
      - postgres
      - redis
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - homelab-net
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M

  # -----------------------------------------------------------------------------
  # PostgreSQL Database
  # -----------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: cybervault-db
    environment:
      - POSTGRES_DB=cybervault
      - POSTGRES_USER=cybervault
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-cybervault_secret}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cybervault"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - homelab-net
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.25"
          memory: 256M

  # -----------------------------------------------------------------------------
  # Redis (Cache & Sessions)
  # -----------------------------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: cybervault-redis
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru --requirepass ${REDIS_PASSWORD:-cybervault_redis_secret}
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-cybervault_redis_secret}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - homelab-net
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
        reservations:
          cpus: "0.1"
          memory: 64M

# =============================================================================
# Networks
# =============================================================================
networks:
  homelab-net:
    external: true

# =============================================================================
# Volumes
# =============================================================================
volumes:
  postgres-data:
    name: cybervault-postgres
  redis-data:
    name: cybervault-redis
